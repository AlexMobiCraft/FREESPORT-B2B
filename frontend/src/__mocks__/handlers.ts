/**
 * MSW (Mock Service Worker) Handlers
 *
 * Этот файл является основным экспортом для всех MSW хендлеров.
 * Фактические хендлеры определены в ./api/handlers.ts для централизации и избежания дублирования.
 */

// Реэкспортируем все хендлеры из api/handlers
export * from './api/handlers';
